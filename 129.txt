server:
  port: 8088

spring:
  batch:
    jdbc:
      initialize-schema: embedded
    job:
      enabled: false

  datasource:
    dynamic:
      primary: master
      strict: false
      datasource:
        # 源数据库 - SQL Server
        master:
          driver-class-name: com.microsoft.sqlserver.jdbc.SQLServerDriver
          # socketTimeout=600000：Socket 读超时10分钟，防止大查询被切断
          # loginTimeout=30：登录超时30秒
          url: jdbc:sqlserver://10.5.80.179:1433;databaseName=OctobackFTMig;encrypt=true;trustServerCertificate=true;rewriteBatchedStatements=true;socketTimeout=600000;loginTimeout=30
          username: Octo
          password: 49573481
          hikari:
            maximum-pool-size: 20
            minimum-idle: 10
            # 连接保活：每30秒心跳，防止防火墙断开空闲连接
            keepalive-time: 30000
            # 空闲连接超时：10分钟
            idle-timeout: 600000
            # 连接最大存活时间：30分钟
            max-lifetime: 1800000
            # 连接获取超时：30秒
            connection-timeout: 30000
            # 连接有效性检测 SQL
            connection-test-query: SELECT 1
            validation-timeout: 5000

        # 目标数据库 - Oracle
        oracle:
          driver-class-name: oracle.jdbc.OracleDriver
          url: jdbc:oracle:thin:@//10.11.198.16:1721/MSEUZ?rewriteBatchedStatements=true
          username: JTQAMSSAPP
          password: JTQAMSSAPP!
          hikari:
            maximum-pool-size: 25
            minimum-idle: 10
            # 连接保活：每30秒对空闲连接发一次心跳，防止 Oracle/防火墙主动断开
            keepalive-time: 30000
            # 空闲连接超时：超过10分钟未使用则回收（需小于 Oracle sqlnet 超时）
            idle-timeout: 600000
            # 连接最大存活时间：30分钟强制重建，防止老连接积累问题
            max-lifetime: 1800000
            # 连接获取超时：30秒内拿不到连接则报错
            connection-timeout: 30000
            # 连接有效性检测 SQL
            connection-test-query: SELECT 1 FROM DUAL
            # 在连接从池中取出前验证有效性
            validation-timeout: 5000

# MyBatis Plus 生产环境配置
mybatis-plus:
  configuration:
    # 生产环境关闭SQL日志输出，提升性能
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
    #    log-impl: org.apache.ibatis.logging.nologging.NoLoggingImpl
    # 批量操作优化
    default-executor-type: SIMPLE
    # 缓存配置
    cache-enabled: true
    lazy-loading-enabled: false
    aggressive-lazy-loading: false
    # 超时配置
    default-statement-timeout: 300  # 5分钟
  global-config:
    db-config:
      # 批量插入大小
      batch-size: 2000

# 日志配置 - 生产环境
logging:
  level:
    root: INFO
    org.example: INFO
    org.springframework.batch: INFO
    # 生产环境减少日志输出
    com.baomidou.dynamic: WARN
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n"
    file: "%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n"
  file:
    name: logs/application.log
    max-size: 100MB
    max-history: 30
